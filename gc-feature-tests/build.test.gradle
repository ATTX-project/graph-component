plugins {
    id "java"
}
// in case we want have a ./gradlw command.
task wrapper(type: Wrapper) {
   gradleVersion = '3.3'
}

dependencies {
    testRuntime  \
        fileTree(dir: 'build/libs', include: ['*.jar'])
}